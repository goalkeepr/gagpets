services:
  node:
    build: .
    environment:
      - NODE_ENV=development
    ports:
      - "8029:8029"
    restart: always
    # Increase shared memory for Chrome/Puppeteer
    shm_size: 1gb
    # Security options to help Chrome run in container
    security_opt:
      - seccomp:unconfined
